{
  "name": "trade-marketing-tools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp -R index.html assets dsm image-tools public/"
  },
  "dependencies": {
    "googleapis": "^144.0.0"
  }
}
